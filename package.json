{
  "name": "MassCommJournal",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "react-scripts": "^4.0.3",
    "gh-pages": "^3.2.3", 
    "framer-motion": "^6.2.4",
    "lucide-react": "^0.14.0",
    "date-fns": "^2.25.0",
    "react-router-dom": "^5.1.0",  // (if using React Router for navigation)
    "@/components/ui/button": "^1.0.0",  // adjust to your actual libraries
    "@/lib/utils": "^1.0.0"  // adjust if using your own utils
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://schackattack89.github.io/MassCommJournal",
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
